import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter', //  ID de tu aplicación
  appName: 'AppFirebase',
  webDir: 'www'
};

export default config;
